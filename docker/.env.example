# Template for docker/.env (gitignored). Copy this file to docker/.env and
# fill in the real values before running `docker compose up`.

# Root password for the MySQL container.
MYSQL_ROOT_PASSWORD=change-me

# Host port exposed for MySQL. Keep 3306 unless another MySQL is already
# bound to that port on the host machine.
MYSQL_HOST_PORT=3306
